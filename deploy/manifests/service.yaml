apiVersion: v1
kind: Service
metadata:
  name: bot-detection
  labels:
    app: bot-detection
spec:
  type: ClusterIP
  selector:
    app: bot-detection
  ports:
    - name: http
      port: 80
      targetPort: 8080
